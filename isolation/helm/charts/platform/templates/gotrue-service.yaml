apiVersion: v1
kind: Service
metadata:
  name: gotrue
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "platform.labels" . | nindent 4 }}
    app: gotrue
spec:
  selector:
    app: gotrue
  ports:
    - name: http
      port: 9999
      targetPort: http
  type: ClusterIP
